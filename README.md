Install dependencies:

```
npm install
```

Build Semantic UI files:

```
cd semantic
gulp build
```

Run on localhost:8080:

```
npm start
```

Test with Mocha:

```
npm test
```

Deploy:

```
npm run deploy
```
